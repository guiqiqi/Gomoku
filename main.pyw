"""Run game"""

# import argparse
# from controller import Gomoku

# parser = argparse.ArgumentParser(description="Game board size and grids.")
# parser.add_argument("--size", type=int, default=500,
#                     help="Game board size (without padding).")
# parser.add_argument("--grids", type=int, default=15,
#                     help="Number of game board grids.")
# args = parser.parse_args()
# game = Gomoku(args.size, args.grids)
# game.start()
